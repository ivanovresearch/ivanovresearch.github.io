# ivanovresearch.dev

Source of [ivanovresearch.dev](https://ivanovresearch.dev) — a single
self-contained static page, deployed via GitHub Pages (branch `main`, root).
The `CNAME` file binds the custom domain.

To update: edit `index.html`, push to `main`. No build step.

The canonical project this site describes:
[senrah](https://github.com/ivanovresearch/senrah).
